{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=server --filter=cc-computer",
    "dev:sever": "turbo run dev --filter=server",
    "dev:fake": "turbo run dev --filter=server --filter=fake-server",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "the-brain-mono"
}